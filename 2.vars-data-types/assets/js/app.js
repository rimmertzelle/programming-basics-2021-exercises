// 1. Declaring with variables
// Declare the following variables in JavaScript: `firstName`, `lastName` and `age`. Initialise the variables with your own content. You may use string concatenation or a template string (``Hello, I am ${firstName} ${lastName} and I am ${age} years old``). Now display the variables in a single string and print the string to the console.

// 2. calculate
let priceOfAnApple = "0.59";
let numberOfApples = 200;

console.log(priceOfAnApple * numberOfApples);

// 3. Pythogarean Theorom
let a = 5,
  b = 6,
  c = undefined;
c = Math.sqrt(a * a + b * b);
console.log(c);

// 4. Strings
const sentence = 'Please visit HZ university of applied science';
const sentence2 = sentence.replace('applied','mad');

console.log(sentence2);